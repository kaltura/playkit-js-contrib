{
  "name": "@playkit-js/playkit-js-ovp",
  "private": false,
  "version": "1.0.0-rc.1",
  "license": "AGPL-3.0",
  "author": "Kaltura",
  "repository": {
    "type": "git",
    "url": "https://github.com/kaltura/playkit-js-ovp"
  },
  "scripts": {
    "type-check": "tsc --noEmit",
    "serve": "npm run type-check -- --watch",
    "build": "webpack --mode production && npm run build:declaration",
    "build:declaration": "dts-bundle-generator --config dts-config.js",
    "postinstall": "npm-install-peers",
    "release": "standard-version --no-verify",
    "release:rc": "npm run release -- --prerelease rc",
    "deploy": "npm run build && git push --follow-tags origin master && npm publish",
    "deploy:rc": "npm run build && git push --follow-tags origin master && npm publish --tag next"
  },
  "main": "./lib/index.js",
  "types": "./lib/index.d.ts",
  "devDependencies": {
    "dts-bundle-generator": "^3.0.0",
    "fs-extra": "^7.0.1",
    "husky": "^1.3.1",
    "lint-staged": "^8.1.5",
    "npm-install-peers": "^1.2.1",
    "peer-deps-externals-webpack-plugin": "^1.0.4",
    "prettier": "^1.16.4",
    "standard-version": "^5.0.2",
    "ts-loader": "^5.3.3",
    "typescript": "^3.2.2",
    "webpack": "^4.29.6",
    "webpack-cli": "^3.3.0",
    "webpack-merge": "^4.2.1",
    "webpack-node-externals": "^1.7.2"
  },
  "lint-staged": {
    "*.{ts,tsx,json,css,html}": [
      "prettier --write",
      "git add"
    ]
  },
  "peerDependencies": {
    "preact": "^8.4.2"
  }
}
