{
    "packages": ["packages/*"],
    "npmClient": "npm",
    "command": {
        "publish": {
            "allowBranch": "master",
            "ignoreChanges": ["ignored-file", "*.md"]
        }
    },
    "version": "0.1.0"
}
